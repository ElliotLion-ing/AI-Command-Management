# 🎯 快速答案：发布几个版本？

## 核心答案

```
运行 ./publish.sh → 发布 1 个 npm 包 → 用户得到 2 个命令
```

---

## 📦 发布内容

```
┌─────────────────────────────────────────────┐
│                                             │
│   NPM 包: @elliotding/ai-command-tool-mcp   │
│   版本: 0.0.3 ← 只有一个版本号               │
│                                             │
│   ┌───────────────────────────────────┐    │
│   │  dist/                            │    │
│   │  ├── index.js      (54 KB)        │    │
│   │  │   └→ stdio 模式实现             │    │
│   │  │                                 │    │
│   │  └── index-sse.js  (53 KB)        │    │
│   │      └→ SSE 模式实现               │    │
│   └───────────────────────────────────┘    │
│                                             │
│   package.json (定义命令映射):              │
│   {                                         │
│     "bin": {                                │
│       "ai-command-tool": "dist/index.js",   │
│       "ai-command-tool-server": "dist/index-sse.js" │
│     }                                       │
│   }                                         │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 🔄 安装过程

```
用户执行:
  npm install -g @elliotding/ai-command-tool-mcp

        ↓

npm 下载 1 个包:
  /usr/local/lib/node_modules/@elliotding/ai-command-tool-mcp/
  ├── dist/index.js
  ├── dist/index-sse.js
  └── package.json

        ↓

npm 自动创建 2 个命令:
  
  /usr/local/bin/ai-command-tool
    ↓ (符号链接)
    → .../ai-command-tool-mcp/dist/index.js
  
  /usr/local/bin/ai-command-tool-server
    ↓ (符号链接)
    → .../ai-command-tool-mcp/dist/index-sse.js

        ↓

用户可以使用 2 个命令:
  ✅ ai-command-tool          (stdio 模式)
  ✅ ai-command-tool-server   (SSE 模式)
```

---

## ❓ 不是两个包，不是启动参数

### ❌ 不是这样（两个独立的包）

```
npm install @elliotding/ai-command-tool-stdio    ← 没有这个包
npm install @elliotding/ai-command-tool-sse      ← 没有这个包
```

### ❌ 不是这样（同一个命令的不同参数）

```bash
ai-command-tool --mode=stdio    ← 不是这样
ai-command-tool --mode=sse      ← 不是这样
```

### ✅ 实际是这样（一个包，两个命令）

```bash
# 安装一个包
npm install -g @elliotding/ai-command-tool-mcp

# 自动得到两个不同的命令
ai-command-tool         # 命令 1，运行 dist/index.js
ai-command-tool-server  # 命令 2，运行 dist/index-sse.js
```

---

## 🎭 类比说明

### 就像 TypeScript

```bash
npm install -g typescript    # 安装 1 个包

# 自动得到 2 个命令
tsc       # TypeScript 编译器
tsserver  # TypeScript 语言服务器
```

### 就像 npm 自己

```bash
npm install -g npm    # 安装 1 个包

# 自动得到 2 个命令
npm       # npm 包管理器
npx       # npm 包执行器
```

### 你的包也是这样

```bash
npm install -g @elliotding/ai-command-tool-mcp    # 安装 1 个包

# 自动得到 2 个命令
ai-command-tool        # stdio 模式 MCP 服务器
ai-command-tool-server # SSE 模式 MCP 服务器
```

---

## 📊 数字总结

| 项目 | 数量 |
|------|------|
| **发布的 npm 包** | **1 个** |
| **版本号** | **1 个** (0.0.3) |
| **构建的 JS 文件** | **2 个** (index.js + index-sse.js) |
| **提供的命令** | **2 个** (ai-command-tool + ai-command-tool-server) |
| **用户安装次数** | **1 次** |
| **用户可用命令** | **2 个** |

---

## ✅ 最终答案

### 运行 `./publish.sh` 会：

1. ✅ 发布 **1 个 npm 包**
2. ✅ 使用 **1 个版本号**（例如 0.0.3）
3. ✅ 包含 **2 个 JS 文件**（index.js 和 index-sse.js）
4. ✅ 自动提供 **2 个全局命令**
5. ✅ 用户只需安装 **1 次**

### 不是：

- ❌ 不是发布 2 个独立的包
- ❌ 不是需要发布 2 次
- ❌ 不是用户需要安装 2 次
- ❌ 不是同一个命令的不同启动参数

### 原理：

**npm 的 `bin` 字段**允许一个包定义多个可执行命令，这是 npm 的标准功能！

